# Set DEMO_MODE=true to test the React/FastAPI workflow without AWS/Terraform/Ansible.
DEMO_MODE=false

# These are resolved relative to the project root by default.
# TERRAFORM_DIR=/absolute/path/to/your/terraform
# ANSIBLE_DIR=/absolute/path/to/your/ansible
# ANSIBLE_INVENTORY=inventory.aws_ec2.yaml
# ANSIBLE_PLAYBOOK=site.yaml
