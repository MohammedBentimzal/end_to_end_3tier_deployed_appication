server {
    listen 80; 
    
    location / {
        proxy_pass http://10.0.2.10:{{ backend_port }}; 
    }
    location /health {
        proxy_pass http://10.0.2.10:{{ backend_port }}/health;
    }
    location /api/info {
        proxy_pass http://10.0.2.10:{{ backend_port }}/api/info;
    }
}